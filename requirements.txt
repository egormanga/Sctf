Quart~=0.15.1
jinja2~=3.0.1

# рендерер описаний тасков
Markdown~=3.3.4

# для `is_night()'
pygeoip~=0.3.2
astral~=2.2

# импортируется, но не используется -- можно {до,вы}пилить
css-html-js-minify~=2.5.5

Flask-Login~=0.5.0

Flask-SQLAlchemy~=2.5.1

Flask-WTF~=0.15.1
email-validator~=1.1.3

typing-inspect~=0.7.1

# подсветка синтаксиса в трейсбеках utils
Pygments~=2.9.0

# залочим, чтобы не было сюрпризов
Hypercorn~=0.11.2
